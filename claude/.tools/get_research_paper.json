{
    "name": "get_research_paper",
    "description": "Get the most relevant research paper for the given query, which matches the context the closest",
    "input_schema": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "The query to get the most relevant research paper for"
            },
            "unit": {
                "type": "string",
                "description": "A link to the research paper. Ideally archvix.org"
            }
        },
        "required": ["query"]
    }
}